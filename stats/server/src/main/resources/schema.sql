CREATE TABLE  IF NOT EXISTS hits
(
    id        BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app       VARCHAR(32),
    uri       VARCHAR(128),
    ip        VARCHAR(16),
    time_hits TIMESTAMP WITHOUT TIME ZONE NOT NULL
    );